export default function TvPage() {
  return (
    <div className="fixed inset-0 flex flex-col items-center justify-center bg-stage text-paper">
      <h1 className="font-display text-[6vmin] font-bold">Display</h1>
      <p className="mt-[2vmin] text-[2vmin] text-paper/50">
        Waiting for the SSE stream — not built yet.
      </p>
    </div>
  )
}
