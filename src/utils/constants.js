export const ROUTES = {
  // Tablet — the guest-facing capture flow.
  form: '/',
  template: '/template',
  capture: '/capture',
  sent: '/sent',
  // TV — the display, opened on its own screen.
  tv: '/tv',
}

export const STORAGE_KEY = 'kiosk.session.v1'
